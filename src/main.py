from .parser import *


def main():

    # parse()

    links = build_dict()
    # extracted_links = [[i for i in links[key]] for key in links.keys()]

    # for link in links.values():
    #     print(len(link))

    # teacher = 3504
    # classes = 2534
    # rooms = 986